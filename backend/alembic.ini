[alembic]
script_location = %(here)s/alembic
prepend_sys_path = %(here)s
path_separator = os

# Connections are supplied only by scripts/migrate_foundation.py.
# This history manages foundation_* tables, not the legacy schema.

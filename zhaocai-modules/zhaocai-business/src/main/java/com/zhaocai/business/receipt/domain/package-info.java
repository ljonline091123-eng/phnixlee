/**
 * 履约事实镜像域。
 *
 * <p>本包中的 Receipt* 对象记录外部履约系统同步的采购订单、收货、发票、对账、结算和台账事实，
 * 不属于采购、招标、投标、评标、定标或合同聚合根。外部事实允许被核心域查询和核对，但不得从本包
 * 反向修改招标结果、定标决策或采购合同主状态；跨域变化必须经显式应用服务和可审计的集成事件完成。</p>
 */
package com.zhaocai.business.receipt.domain;
